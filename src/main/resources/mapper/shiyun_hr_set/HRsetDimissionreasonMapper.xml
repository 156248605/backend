<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.elex.oa.dao.dao_shiyun.IHRsetDimissionreasonDao">

	<resultMap id="DimissionreasonMapper" type="HRsetDimissionreason">
		<id column="id" property="id" />
		<result column="dimissionreason" property="dimissionreason" />
	</resultMap>

	<insert id="insertOne" parameterType="HRsetDimissionreason" useGeneratedKeys="true" keyProperty="id">
		insert into tb_hr_set_dimissionreason (
			id,
			dimissionreason)
		values (
			NULL,
			#{dimissionreason})
	</insert>

	<select id="selectAll" resultMap="DimissionreasonMapper">
		SELECT * FROM tb_hr_set_dimissionreason
	</select>

	<select id="selectByConditions" parameterType="HRsetDimissionreason" resultMap="DimissionreasonMapper">
		SELECT * FROM tb_hr_set_dimissionreason
		<where>
			<if test="id!=null and id!=''">AND id=#{id}</if>
			<if test="dimissionreason!=null and dimissionreason!=''">AND dimissionreason=#{dimissionreason}</if>
		</where>
	</select>

	<delete id="deleteOne" parameterType="java.lang.Integer">
		DELETE FROM tb_hr_set_dimissionreason WHERE id=#{id}
	</delete>

	<update id="updateOne" parameterType="HRsetDimissionreason">
		UPDATE tb_hr_set_dimissionreason
		SET dimissionreason=#{dimissionreason}
		WHERE id=#{id}
	</update>

	<select id="selectById" parameterType="java.lang.Integer" resultMap="DimissionreasonMapper">
		SELECT * FROM tb_hr_set_dimissionreason
		WHERE id=#{id}
	</select>

	<select id="selectByHR" parameterType="java.lang.String" resultMap="DimissionreasonMapper">
		SELECT * FROM tb_hr_set_dimissionreason
		WHERE dimissionreason=#{dimissionreason}
	</select>
</mapper>
