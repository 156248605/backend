<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.elex.oa.dao.dao_shiyun.IHRsetDimissiondirectionDao">

	<resultMap id="DimissiondirectionMapper" type="HRsetDimissiondirection">
		<id column="id" property="id" />
		<result column="dimissiondirection" property="dimissiondirection" />
	</resultMap>

	<insert id="insertOne" parameterType="HRsetDimissiondirection" useGeneratedKeys="true" keyProperty="id">
		insert into tb_hr_set_dimissiondirection (
			id,
			dimissiondirection)
		values (
			NULL,
			#{dimissiondirection})
	</insert>

	<select id="selectAll" resultMap="DimissiondirectionMapper">
		SELECT * FROM tb_hr_set_dimissiondirection
		ORDER BY id DESC
	</select>

	<select id="selectByConditions" parameterType="HRsetDimissiondirection" resultMap="DimissiondirectionMapper">
		SELECT * FROM tb_hr_set_dimissiondirection
		<where>
			<if test="id!=null and id!=''">AND id=#{id}</if>
			<if test="dimissiondirection!=null and dimissiondirection!=''">AND dimissiondirection=#{dimissiondirection}</if>
		</where>
	</select>

	<delete id="deleteOne" parameterType="java.lang.Integer">
		DELETE FROM tb_hr_set_dimissiondirection WHERE id=#{id}
	</delete>

	<update id="updateOne" parameterType="HRsetDimissiondirection">
		UPDATE tb_hr_set_dimissiondirection
		SET dimissiondirection=#{dimissiondirection}
		WHERE id=#{id}
	</update>

	<select id="selectById" parameterType="java.lang.Integer" resultMap="DimissiondirectionMapper">
		SELECT * FROM tb_hr_set_dimissiondirection
		WHERE id=#{id}
	</select>

	<select id="selectByHR" parameterType="java.lang.String" resultMap="DimissiondirectionMapper">
		SELECT * FROM tb_hr_set_dimissiondirection
		WHERE dimissiondirection=#{dimissiondirection}
	</select>
</mapper>
